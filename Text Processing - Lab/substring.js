function substring(input, startIndex, count) {

    let result = input.substring(startIndex, count + startIndex);
    console.log(result);
}

substring('ASentence', 1, 8);
substring('SkipWord', 4, 7)
