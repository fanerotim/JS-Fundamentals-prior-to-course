function solve(arr) {

    let countries = [];

    for (let line of arr) {
        let [country, town, cost] = line.split(' > ');
        cost = Number(cost);

         
    }
}

solve([
    "Bulgaria > Sofia > 500",
    "Bulgaria > Sofia > 200",
    // "Bulgaria > Sopot > 800",
    // "France > Paris > 2000",
    // "Albania > Tirana > 1000",
    // "Bulgaria > Sofia > 200"
]);